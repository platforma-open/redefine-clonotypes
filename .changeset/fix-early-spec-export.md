---
'@platforma-open/milaboratories.redefine-clonotypes.workflow': minor
'@platforma-open/milaboratories.redefine-clonotypes': minor
---

Fix early spec export: replace wf.prepare with PColumnBundle await so downstream blocks can configure inputs before computation completes
